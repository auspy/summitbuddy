import type { Metadata, Viewport } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Summit Buddy | India AI Impact Summit 2026",
  description:
    "Your AI guide to the India AI Impact Summit 2026. Get personalized session recommendations, exhibitor suggestions, and schedule planning.",
  keywords: [
    "India AI Summit",
    "AI Impact Summit 2026",
    "session recommendations",
    "Bharat Mandapam",
    "GPAI",
  ],
  openGraph: {
    title: "Summit Buddy | India AI Impact Summit 2026",
    description:
      "AI-powered session navigator for the world's largest AI summit",
    type: "website",
  },
};

export const viewport: Viewport = {
  width: "device-width",
  initialScale: 1,
  maximumScale: 1,
  themeColor: [
    { media: "(prefers-color-scheme: light)", color: "#ffffff" },
    { media: "(prefers-color-scheme: dark)", color: "#0a0a0a" },
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
